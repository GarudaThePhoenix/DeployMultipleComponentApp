pr:
  branches:
    include:
    - master

  paths:
    include:
    - FirstFunctionApp/*
    - Swagger/*
    
pool:
  vmImage: 'ubuntu-16.04'
  
variables:
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Debug'
  projectName: 'FirstFunctionApp/FirstFunctionApp.csproj'
  
steps:
- task: DotNetCoreCLI@2
  inputs:
    command: build
    projects: $(projectName)
    configuration: $(buildConfiguration)
    
